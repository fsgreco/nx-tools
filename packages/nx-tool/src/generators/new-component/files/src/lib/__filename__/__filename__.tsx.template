import React, { FC } from 'react';
import styled from 'styled-components'

const Div = styled.div`  `

/* eslint-disable-next-line */
interface Props {
	/** This will be the description in storybook */
	label?: string
}

export const <%= name %>: FC<Props> = ({label}) => {

  return (
		<Div>
			{label} <%= name %>
		</Div>
	);
};

export default <%= name %>;